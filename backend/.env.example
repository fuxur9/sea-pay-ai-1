# SeaPay Backend Configuration

# Ethereum Private Key (REQUIRED)
# Used for both:
# 1. x402 payment processing (fallback compatibility)
# 2. CDP Smart Wallet owner authentication (AgentKit)
# Format: 0x followed by 64 hex characters, or just 64 hex characters
PRIVATE_KEY=your_ethereum_private_key_here

# SeaPay API Base URL
# Optional: defaults to the ngrok tunnel URL if not set
SEAPAY_API_BASE_URL=your_api_url
OPENAI_API_KEY=your_open_ai_api_key

# Coinbase CDP Configuration (for on-chain wallet features)
# Get these from: https://portal.cdp.coinbase.com/access/api
CDP_API_KEY_ID=your_cdp_api_key_id
CDP_API_KEY_SECRET=your_cdp_api_key_secret

# Wallet Configuration
# Option 1: Use existing wallet secret (recommended for persistent wallet)
CDP_WALLET_SECRET=your_cdp_wallet_secret

# Option 2: Let AgentKit create new wallet on first run (development)
# Leave CDP_WALLET_SECRET empty to auto-create a new wallet

# Network Configuration
NETWORK_ID=base-sepolia  # or base-mainnet, ethereum-mainnet, etc.
